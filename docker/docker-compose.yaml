version: '3'

services:
    nginx:
        container_name: nginx
        depends_on:
            - xilution-graphql-example
        image: nginx:1.17.2
        networks:
            - xilution-graphql-example
        ports:
            - '80:80'
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf
    xilution-graphql-example:
        build: .
        container_name: xilution-graphql-example
        environment:
            - XILUTION_ENVIRONMENT
            - XILUTION_CLIENT_SECRET
            - XILUTION_CLIENT_ID
            - XILUTION_USER_ID
        image: xilution-graphql-example
        ports:
            - '3000'
        networks:
            - xilution-graphql-example

networks:
    xilution-graphql-example:
        driver: bridge
